<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".IMDMarketApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IMDMarket"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.products.CreateActivity"
            android:exported="false"
            android:label="@string/title_activity_products_create"
            android:theme="@style/Theme.IMDMarket" />
        <activity
            android:name=".ui.activities.products.DeleteActivity"
            android:exported="false"
            android:label="@string/title_activity_products_delete"
            android:theme="@style/Theme.IMDMarket" />
        <activity
            android:name=".ui.activities.products.EditActivity"
            android:exported="false"
            android:label="@string/title_activity_products_edit"
            android:theme="@style/Theme.IMDMarket" />
        <activity
            android:name=".ui.activities.products.IndexActivity"
            android:exported="false"
            android:label="@string/title_activity_products_index"
            android:theme="@style/Theme.IMDMarket" />
        <activity
            android:name=".ui.activities.auth.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_auth_login"
            android:theme="@style/Theme.IMDMarket" />
        <activity
            android:name=".ui.activities.auth.ForgotActivity"
            android:exported="false"
            android:label="@string/title_activity_auth_forgot"
            android:theme="@style/Theme.IMDMarket" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.IMDMarket">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>